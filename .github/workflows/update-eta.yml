name: Schedule Test

on:
  schedule:
    - cron: '55 9 * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduled run
        run: |
          echo "Schedule worked"
          date -u
