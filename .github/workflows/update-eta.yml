name: Schedule Test

on:
  schedule:
    - cron: '42 9 * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show schedule test
        run: |
          echo "Scheduled workflow ran successfully"
          date -u
